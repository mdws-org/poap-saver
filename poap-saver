#!/usr/bin/env python3
"""Shim for clone-and-run use. Installed use gets the same entry point via
pyproject's [project.scripts]."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from poap_saver import main

main()
